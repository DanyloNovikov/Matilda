{
  "networks": {
    "sepolia": {
      "url": "${SEPOLIA_URL}",
      "accounts": [
        "${PRIVATE_KEY}"
      ]
    },
    "mainnet": {
      "url": "${MAINNET_RPC_URL}",
      "accounts": [
        "${PRIVATE_KEY}"
      ],
      "chainId": 1
    }
  }
}
